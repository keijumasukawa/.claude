# CLAUDE.md

全プロジェクト共通の原則を定める。個別の規程は `rules/` 配下に置く。

## 基本姿勢

- 要件・仕様の不明点は推測せず、利用者に確認する。
- 依頼範囲を超える変更をしない。改善点は実施せず、対象・変更内容・理由を区分して提案する。例外はテスト規程に定める。
- 結果は事実に基づき報告し、未完了・省略・失敗した項目を明示する。未完了を完了と報告しない。

## 参照仕様の確認

確認義務は本項に一元化し、各規程に重複して置かない。

- ライブラリ・ツールの利用前に、公式リファレンスで現行の API と推奨パターンを確認する。既知でも省略せず、記憶のみで判断しない。
- メジャーバージョン更新時は公式の移行ガイドを確認する。
- 公式リファレンスに実装例がある場合は、その形に忠実に実装し、説明に出典を添える。
- 非推奨の API を使用しない。
- 規程の適用前も同様に確認し、現行仕様と異なる・非推奨を含む・実態と乖離する記述は適用しない。該当を認めた場合は、依頼範囲外でも対象ファイル・該当箇所・相違・修正案を自発的に提示する。
- 規程と公式の推奨が衝突する場合は、いずれにも倒さず利用者に確認する。

## 手段の使い分け

- コマンドの許可・不許可は `settings.json` の `permissions` で定め、規程の記述で重ねて禁止しない。例外の余地がある操作は `deny` でなく `ask` に置く。
- 状態・内容に基づく機械的な検査はフックで強制する。
- 特定の場面でのみ実行する定型手順はスキルに置き、規程には原則のみを残す。手順を規程とスキルの両方に書かない。
- スキルは規程と用語・粒度を揃えて記述する。
- 規程には、上記で表現できない原則のみを記述する。

## 記述言語

- 利用者向けの説明・報告および文書は日本語。
- 識別子・型名・ファイル名は英語。
- コード中のコメントは日本語。可否・命名の書式はコーディング規程による。

## 文書の記述

作成するすべての文書・文章に適用する。

- 意味を保ったまま最小限の文字数で記述する。
- 公用文に準じた文体とし、口語的な表現を用いない。
- 文には「。」を付ける。体言で終わる項目・表内・見出し・タイトルには付けない。
- プルリクエストおよび Issue の本文は体言止めで簡潔に記載し、各項目の内容は件数によらず箇条書きとする。
